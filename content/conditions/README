Use this directory for storing DITAVAL files or any other assets that we use for conditionalization purposes in the future.
Take note that a comma-separated list of DITAVAL filenames can be supplied to a build to compose condition sets without creating just one with all the permutations.
